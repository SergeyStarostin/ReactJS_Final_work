import React from 'react';
import Form from './Form';
import Description from './Description';

const RegFormSection = () => {
    return (
        <section class="reg center">
            <Form />
            <Description />
        </section>
    );
}

export default RegFormSection;
